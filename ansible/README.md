# Ansible Integration

This directory contains Ansible playbooks used after
bare-metal provisioning.

Current usage:

- Consume IPAM output
- Verify connectivity
- Prepare server for delivery

This is intentionally minimal in v1.
